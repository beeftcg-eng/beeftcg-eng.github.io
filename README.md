# Beef's Workshop

Portfolio site, served by GitHub Pages at https://beeftcg-eng.github.io/.

It's a single static `index.html` (fonts from Google Fonts, nothing else external). Edit it and push to `main`; Pages redeploys in a minute or two.
